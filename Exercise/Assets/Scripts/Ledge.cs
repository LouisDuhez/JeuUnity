using UnityEngine;

public class Ledge : MonoBehaviour
{
    // Distance entre le mur et le joueur (pour ne pas rentrer dans le mur)
    public float forwardOffset = 0.5f;
    // Hauteur de l'accroche par rapport au haut du cube
    public float verticalOffset = 0.5f;
}